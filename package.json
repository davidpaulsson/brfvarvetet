{
  "name": "brf-varvetet",
  "version": "",
  "scripts": {
    "start": "parcel index.html",
    "build":
      "rm -rf dist/ && parcel build index.html --public-url ./ && cp CNAME dist",
    "deploy":
      "push-dir --dir=dist --branch=gh-pages --cleanup --force --verbose"
  },
  "dependencies": {},
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
