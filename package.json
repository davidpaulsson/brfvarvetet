{
  "name": "brf-varvetet",
  "version": "",
  "scripts": {
    "start": "parcel index.html",
    "build": "rm -rf dist/ && parcel build index.html --public-url ./ && cp CNAME dist",
    "deploy": "yarn run build && push-dir --dir=dist --branch=gh-pages"
  },
  "dependencies": {},
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
